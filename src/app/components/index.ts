import { FormComponent } from './form/form.component';
import { ListComponent } from './list/list.component';

export { FormComponent, ListComponent };

export const Components = [FormComponent, ListComponent];
